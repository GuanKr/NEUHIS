<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="neu.his.dao.TranslateMapper" >

    <select id="translate_title" parameterType="java.lang.Integer" resultType="String">
        select title_name from title where title.id = #{id}
    </select>

    <select id="translate_role" parameterType="java.lang.Integer" resultType="String">
        select role_name from role where role.id = #{id}
    </select>

    <select id="translate_department" parameterType="java.lang.Integer" resultType="String">
        select department_name from department where department.id = #{id}
    </select>

    <select id="translate_registration_level" parameterType="java.lang.Integer" resultType="String">
        select registration_level.level_name from registration_level where registration_level.id = #{id}
    </select>

    <select id="de_translate_title" parameterType="java.lang.String" resultType="Integer">
        select title.id from title where title.title_name = #{name}
    </select>

    <select id="de_translate_role" parameterType="java.lang.String" resultType="Integer">
        select role.id from role where role.role_name = #{name}
    </select>

    <select id="de_translate_department" parameterType="java.lang.String" resultType="Integer">
        select department.id from department where department.department_name = #{name}
    </select>

    <select id="de_translate_registration_level" parameterType="java.lang.String" resultType="Integer">
        select registration_level.id from registration_level where registration_level.level_name = #{name}
    </select>

</mapper>